# Lido grocery store — Assignment 1

Local HTML project about Lido at Uly Dala Avenue, 31, Astana.

## Pages and team

| Student responsible | Files inside Grocery/ |
| --- | --- |
| Saparali Shalkar | delivery.html, contacts.html, colophon.html |
| Temirlan | index.html, products.html, prices.html |

Open `Grocery/delivery.html` directly in Safari or another browser. The three
Saparali pages contain a menu linking all six files. There is no server, domain,
hosting, CSS, JavaScript, framework or build command.

- `delivery.html`: a telephone link, four simple questions to ask about delivery,
  and two existing team photographs. The form and product selectors were removed.
- `contacts.html`: public address and telephone, a table of opening hours,
  the storefront photograph, payment information and an attributed public review.
- `colophon.html`: file organisation, editing, HTML examples, telephone links
  with assistance and sources documented in the repository.

## Telephone link

The delivery page links to `tel:+77479052505`. A compatible device can open its
calling app; the visitor can also dial the number manually. The site does not
send an order, collect customer details, take payment or promise delivery.
Delivery availability and charges must be confirmed with the shop.

The previous form version was saved outside the repository as
`../WEB_before_phone_20260913_224839.zip` before the student requested its removal.
The current version has no form, dropdowns, radio buttons or checkboxes.

The email link on the contact page opens a draft with an empty recipient.
A verified store email address was not found. The visitor must obtain and enter
one before sending; this is not a direct email contact for the shop.

## Collaboration state

The initially mistaken page assignment was corrected. The previous generated
index/products/prices draft was backed up outside the repository as
`../WEB_previous_scope_20260913_222048.zip`.

A fresh GitHub snapshot, displayed as commit
`264d11970946740047523170186b034138aad916`, was downloaded on 2026-09-13.
Temirlan's three HTML files and four photographs were copied from that snapshot
without editing their contents. The three photographs used by Saparali's pages
also have copies in `Grocery/images/` so the existing teammate image paths remain usable.

The shell could not resolve github.com, so `git fetch` did not update local Git
history. The downloaded files are a working-tree snapshot, not a completed pull.
Browser uploads are recorded in the repository history on GitHub. Before making
local commits, synchronise with the current remote branch and keep each student's
contribution separate. Do not recommit the downloaded teammate snapshot as new work.

## Verification

See `VALIDATION.md` for the three new pages' validation and browser results.
`TAG_CHECKLIST.md` gives actual tag and attribute line references for these files.
Regenerate its line numbers after editing HTML.

## Remaining assignment requirements

- The required personal form and all its controls are absent after the requested
  simplification. A telephone link does not fulfil the form requirement. The
  nested list, definition list, div, i and second personal-page abbreviation also
  need attention; the checklist marks actual presence instead of counting deleted code.

- A real quote personally collected by a student, with the speaker's name and
  date. The current quote is a public customer review and does not fulfil that requirement.
- A verified shop email and confirmed delivery information, if the shop provides them.
- Evidence of personal contact and the photographer/date for each team photograph.
  Four image files now exist, but image ownership and who took them are not established here.
- Confirmation of the teammate's listed prices and their collection date; they
  were copied as teammate content, not independently verified by AI.
- The teammate still needs to complete their own form, checks and checklist.
  Their index menu has only Products and Prices links; it needs all six pages.
  Their index also has invalid closing tags and a `center` element, and products
  lacks `main` and a correct viewport. Their pages have not been certified by this validation.
- Task A, Task B, the PDF report, screenshots and a signed, dated hand-drawn sketch.
- Genuine commits from each student's own account across the required three days,
  and preparation for the live defence.


